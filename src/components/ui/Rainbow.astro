<section class="mt-10 gap-4 px-4">
  <h2 class="border-foreground border-b text-6xl text-pretty">Rainbow</h2>

  <div class="flex pt-4">
    <div class="border-foreground border-r">
      <p class="mb-4 max-w-96 text-xl">
        Uses a bobblesort, for create a sorted rainbow based on the hue!
      </p>
      <button
        class="border-foreground cursor-pointer rounded-xl border bg-black px-[1em] py-[0.1em] text-base text-black dark:bg-white dark:text-black"
        id="sortButton"
      >
        Sort it</button
      >
      <button
        class="border-foreground cursor-pointer rounded-xl border px-[1em] py-[0.1em] text-base"
        id="resetButton"
      >
        Reset</button
      >
    </div>

    <div class="min-w-2xl pl-4">
      <div
        class="relative flex h-64 w-full max-w-2xl overflow-hidden rounded-xl border border-black bg-conic/decreasing from-violet-700 via-lime-300 to-violet-700 p-1"
        id="rainbow"
      >
      </div>
    </div>
  </div>
</section>
